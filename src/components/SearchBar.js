import React, { Component } from 'react';

class SearchBar extends Component {
  render() {
    return (
      <input className="App__search-bar" type="text" placeholder="Search Summoner Name"/>
    );
  }
}

export default SearchBar;
